services:
  react_app:
      image: node:20
      container_name: react_app
      working_dir: /app
      volumes:
        - .:/app
      ports:
        - "5173:5173"
      command: sh -c "npm install && npm run dev"